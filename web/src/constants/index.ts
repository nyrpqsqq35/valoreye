/*
const agents = {
  ids: agents.data.map(i=>i.uuid),
  names: agents.data.map(i=>i.displayName)
}
*/

export const PresenceStates = ['Online', 'Offline', 'Away', 'Mobile']

export const TooltipClass =
  '!inline-flex !items-center !px-2.5 !py-0.5 !rounded-full !text-xs !font-medium !border-none !bg-ctp-crust !text-ctp-subtext0'
